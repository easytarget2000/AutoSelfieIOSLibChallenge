default_platform(:ios)

platform :ios do
  xcworkspace = "Example/AutoSelfieIOSLibChallenge.xcworkspace"
  test_scheme = "AutoSelfieIOSLibChallenge-Example"

  desc "Run all framework tests"
    lane :test do
      run_tests(workspace: xcworkspace, scheme: test_scheme)
    end

end
